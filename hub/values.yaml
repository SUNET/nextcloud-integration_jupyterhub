debug:
  enabled: true
hub:
  config:
    Authenticator:
      auto_login: true
      enable_auth_state: true
    JupyterHub:
      tornado_settings:
        headers: { 'Content-Security-Policy': "frame-ancestors *;" }
  db:
    pvc:
      storageClassName: csi-sc-cinderplugin
  extraConfig:
    oauthCode: |
      import time
      import requests
      from datetime import datetime
      from urllib.parse import urlparse
      import jwt
      from jwt import PyJWKClient
      from jupyterhub.handlers.base import BaseHandler
      from oauthenticator.generic import GenericOAuthenticator
      token_url = 'https://' + os.environ['NEXTCLOUD_HOST'] + '/index.php/apps/oauth2/api/v1/token'
      debug = os.environ.get('NEXTCLOUD_DEBUG_OAUTH', 'false').lower() in ['true', '1', 'yes']

      _OCM_TRUSTED_ISSUER_DOMAINS = frozenset(
        d.strip() for d in os.environ.get('OCM_TRUSTED_ISSUER_DOMAINS', '').split(',') if d.strip()
      )
      _ocm_jwks_clients = {}
      def _ocm_jwks(domain):
        client = _ocm_jwks_clients.get(domain)
        if client is None:
          client = PyJWKClient(f'https://{domain}/.well-known/jwks.json', cache_keys=True)
          _ocm_jwks_clients[domain] = client
        return client

      def _verify_ocm_jwt(token):
        try:
          unverified = jwt.decode(token, options={'verify_signature': False})
        except jwt.InvalidTokenError as e:
          raise ValueError(f'malformed token: {e}')
        issuer = unverified.get('iss')
        if not issuer:
          raise ValueError('token missing iss claim')
        parsed = urlparse(issuer)
        if parsed.scheme != 'https' or not parsed.netloc:
          raise ValueError('iss must be an https URL')
        if _OCM_TRUSTED_ISSUER_DOMAINS and parsed.netloc not in _OCM_TRUSTED_ISSUER_DOMAINS:
          raise ValueError(f'issuer {parsed.netloc} not in trusted issuer allowlist')
        signing_key = _ocm_jwks(parsed.netloc).get_signing_key_from_jwt(token).key
        return jwt.decode(
          token, signing_key,
          algorithms=['RS256', 'RS384', 'RS512', 'ES256', 'ES384', 'EdDSA'],
          issuer=issuer,
          options={'require': ['iss', 'sub', 'aud', 'exp', 'client_id'], 'verify_aud': False},
        )

      class OCMLoginHandler(BaseHandler):
        async def post(self):
          access_token = self.get_argument('access_token', '')
          next_url = self.get_argument('next', '/hub/home')
          if not access_token:
            self.set_status(400); self.write('access_token missing'); return
          try:
            claims = _verify_ocm_jwt(access_token)
          except (ValueError, jwt.InvalidTokenError) as e:
            self.set_status(401); self.write(f'OCM token verification failed: {e}'); return
          username = f'ocm:{claims["aud"]}'
          user = await self.auth_to_user({'name': username})
          self.set_login_cookie(user)
          self.redirect(next_url)

      def get_nextcloud_access_token(refresh_token):
        client_id = os.environ['NEXTCLOUD_CLIENT_ID']
        client_secret = os.environ['NEXTCLOUD_CLIENT_SECRET']

        code = refresh_token
        data = {
          'grant_type': 'refresh_token',
          'code': code,
          'refresh_token': refresh_token,
          'client_id': client_id,
          'client_secret': client_secret
        }
        response = requests.post(token_url, data=data)
        if debug:
          print(response.text)
        return response.json()

      def post_auth_hook(authenticator, handler, authentication):
        user = authentication['auth_state']['oauth_user']['ocs']['data']['id']
        auth_state = authentication['auth_state']
        auth_state['token_expires'] =  time.time() + auth_state['token_response']['expires_in']
        authentication['auth_state'] = auth_state
        return authentication

      class NextcloudOAuthenticator(GenericOAuthenticator):
        def __init__(self, *args, **kwargs):
          super().__init__(*args, **kwargs)
          self.user_dict = {}

        def get_handlers(self, app):
          return super().get_handlers(app) + [(r'/ocm-login', OCMLoginHandler)]

        async def pre_spawn_start(self, user, spawner):
          super().pre_spawn_start(user, spawner)
          ocm = (spawner.user_options or {}).get('ocm_share')
          if ocm:
            spawner.environment['OCM_WEBDAV_URI'] = ocm['webdav_uri']
            spawner.environment['OCM_BEARER'] = ocm['bearer']
            spawner.environment['OCM_PERMISSIONS'] = ','.join(ocm.get('permissions', ['read']))
            spawner.environment['OCM_RESOURCE_NAME'] = ocm.get('resource_name', '')
            spawner.environment['OCM_SHARER'] = ocm.get('sharer', '')
            spawner.environment['OCM_PROVIDER_ID'] = ocm.get('provider_id', '')
            extra = spawner.extra_container_config or {}
            sec = dict(extra.get('securityContext') or {})
            caps = dict(sec.get('capabilities') or {})
            caps['add'] = list({*caps.get('add', []), 'SYS_ADMIN'})
            sec['capabilities'] = caps
            extra['securityContext'] = sec
            spawner.extra_container_config = extra
            return
          auth_state = await user.get_auth_state()
          if not auth_state:
            return
          access_token = auth_state['access_token']
          spawner.environment['NEXTCLOUD_ACCESS_TOKEN'] = access_token

        async def refresh_user(self, user, handler=None):
          auth_state = await user.get_auth_state()
          if not auth_state:
            if debug:
              print(f'auth_state missing for {user}')
            return False
          access_token = auth_state['access_token']
          refresh_token = auth_state['refresh_token']
          token_response = auth_state['token_response']
          now = time.time()
          now_hr = datetime.fromtimestamp(now)
          expires = auth_state['token_expires']
          expires_hr = datetime.fromtimestamp(expires)
          expires = 0
          if debug:
            print(f'auth_state for {user}: {auth_state}')
          if now >= expires:
            if debug:
              print(f'Time is: {now_hr}, token expired: {expires_hr}')
              print(f'Refreshing token for {user}')
            try:
              token_response = get_nextcloud_access_token(refresh_token)
              auth_state['access_token'] = token_response['access_token']
              auth_state['refresh_token'] = token_response['refresh_token']
              auth_state['token_expires'] = now + token_response['expires_in']
              auth_state['token_response'] = token_response
              if debug:
                print(f'Successfully refreshed token for {user.name}')
                print(f'auth_state for {user.name}: {auth_state}')
              return {'name': user.name, 'auth_state': auth_state}
            except Exception as e:
              if debug:
                print(f'Failed to refresh token for {user}')
              return False
            return False
          if debug:
            print(f'Time is: {now_hr}, token expires: {expires_hr}')
          return True

      c.JupyterHub.authenticator_class = NextcloudOAuthenticator
      c.NextcloudOAuthenticator.client_id = os.environ['NEXTCLOUD_CLIENT_ID']
      c.NextcloudOAuthenticator.client_secret = os.environ['NEXTCLOUD_CLIENT_SECRET']
      c.NextcloudOAuthenticator.login_service = 'Sunet Drive'
      c.NextcloudOAuthenticator.username_claim = lambda r: r.get('ocs', {}).get('data', {}).get('id')
      c.NextcloudOAuthenticator.userdata_url = 'https://' + os.environ['NEXTCLOUD_HOST'] + '/ocs/v2.php/cloud/user?format=json'
      c.NextcloudOAuthenticator.authorize_url = 'https://' + os.environ['NEXTCLOUD_HOST'] + '/index.php/apps/oauth2/authorize'
      c.NextcloudOAuthenticator.token_url = token_url
      c.NextcloudOAuthenticator.oauth_callback_url = 'https://' + os.environ['JUPYTER_HOST'] + '/hub/oauth_callback'
      c.NextcloudOAuthenticator.allow_all = True
      c.NextcloudOAuthenticator.refresh_pre_spawn = True
      c.NextcloudOAuthenticator.enable_auth_state = True
      c.NextcloudOAuthenticator.auth_refresh_age = 3600
      c.NextcloudOAuthenticator.post_auth_hook = post_auth_hook

    serviceCode: |
      import sys
      c.JupyterHub.load_roles = [
          {
              "name": "refresh-token",
              "services": [
                "refresh-token"
              ],
              "scopes": [
                "read:users",
                "admin:auth_state"
              ]
          },
          {
              "name": "ocm",
              "services": [
                "ocm"
              ],
              "scopes": [
                "admin:users",
                "admin:servers"
              ]
          },
          {
              "name": "user",
              "scopes": [
                "access:services!service=refresh-token",
                "read:services!service=refresh-token",
                "self",
              ],
          },
          {
              "name": "server",
              "scopes": [
                "access:services!service=refresh-token",
                "read:services!service=refresh-token",
                "inherit",
              ],
          }
      ]
      c.JupyterHub.services = [
          {
              'name': 'refresh-token',
              'url': 'http://' + os.environ.get('HUB_SERVICE_HOST', 'hub') + ':' + os.environ.get('HUB_SERVICE_PORT_REFRESH_TOKEN', '8082'),
              'display': False,
              'oauth_no_confirm': True,
              'api_token': os.environ['JUPYTERHUB_API_KEY'],
              'command': [sys.executable, '/usr/local/etc/jupyterhub/refresh-token.py']
          },
          {
              'name': 'ocm',
              'url': 'http://' + os.environ.get('HUB_SERVICE_HOST', 'hub') + ':' + os.environ.get('HUB_SERVICE_PORT_OCM', '8083'),
              'display': False,
              'api_token': os.environ['JUPYTERHUB_OCM_API_KEY'],
              'command': [sys.executable, '/usr/local/etc/jupyterhub/ocm-service.py']
          }
      ]
      c.JupyterHub.admin_users = {"refresh-token", "ocm"}
      c.JupyterHub.api_tokens = {
          os.environ['JUPYTERHUB_API_KEY']: "refresh-token",
          os.environ['JUPYTERHUB_OCM_API_KEY']: "ocm",
      }
  extraFiles:
    refresh-token.py: 
      mountPath: /usr/local/etc/jupyterhub/refresh-token.py
      stringData: |
        """A token refresh service authenticating with the Hub.

        This service serves `/services/refresh-token/`,
        authenticated with the Hub,
        showing the user their own info.
        """
        import json
        import os
        import requests
        import socket
        from jupyterhub.services.auth import HubAuthenticated
        from jupyterhub.utils import url_path_join
        from tornado.httpserver import HTTPServer
        from tornado.ioloop import IOLoop
        from tornado.web import Application, HTTPError, RequestHandler, authenticated
        from urllib.parse import urlparse
        debug = os.environ.get('NEXTCLOUD_DEBUG_OAUTH', 'false').lower() in ['true', '1', 'yes']
        def my_debug(s):
          if debug:
            with open("/proc/1/fd/1", "a") as stdout:
              print(s, file=stdout)


        class RefreshHandler(HubAuthenticated, RequestHandler):
            def api_request(self, method, url, **kwargs):
                my_debug(f'{self.hub_auth}')
                url = url_path_join(self.hub_auth.api_url, url)
                allow_404 = kwargs.pop('allow_404', False)
                headers = kwargs.setdefault('headers', {})
                headers.setdefault('Authorization', f'token {self.hub_auth.api_token}')
                try:
                    r = requests.request(method, url, **kwargs)
                except requests.ConnectionError as e:
                    my_debug(f'Error connecting to {url}: {e}')
                    msg = f'Failed to connect to Hub API at {url}.'
                    msg += f'  Is the Hub accessible at this URL (from host: {socket.gethostname()})?'

                    if '127.0.0.1' in url:
                        msg += '  Make sure to set c.JupyterHub.hub_ip to an IP accessible to' + \
                               ' single-user servers if the servers are not on the same host as the Hub.'
                    raise HTTPError(500, msg)

                data = None
                if r.status_code == 404 and allow_404:
                    pass
                elif r.status_code == 403:
                    my_debug(
                        'Lacking permission to check authorization with JupyterHub,' + 
                        f' my auth token may have expired: [{r.status_code}] {r.reason}'
                    )
                    my_debug(r.text)
                    raise HTTPError(
                        500,
                        'Permission failure checking authorization, I may need a new token'
                    )
                elif r.status_code >= 500:
                    my_debug(f'Upstream failure verifying auth token: [{r.status_code}] {r.reason}')
                    my_debug(r.text)
                    raise HTTPError(
                        502, 'Failed to check authorization (upstream problem)')
                elif r.status_code >= 400:
                    my_debug(f'Failed to check authorization: [{r.status_code}] {r.reason}')
                    my_debug(r.text)
                    raise HTTPError(500, 'Failed to check authorization')
                else:
                    data = r.json()
                return data

            @authenticated
            def get(self):
                user_model = self.get_current_user()
                # Fetch current auth state
                user_data = self.api_request('GET', url_path_join('users', user_model['name']))
                auth_state = user_data['auth_state']
                access_token = auth_state['access_token']
                token_expires = auth_state['token_expires']

                self.set_header('content-type', 'application/json')
                self.write(json.dumps({'access_token': access_token, 'token_expires': token_expires}, indent=1, sort_keys=True))

        class PingHandler(RequestHandler):

            def get(self):
                my_debug(f"DEBUG: In ping get")
                self.set_header('content-type', 'application/json')
                self.write(json.dumps({'ping': 1}))


        def main():
            app = Application([
                (os.environ['JUPYTERHUB_SERVICE_PREFIX'] + 'tokens', RefreshHandler),
                (os.environ['JUPYTERHUB_SERVICE_PREFIX'] + '/?', PingHandler),
            ])

            http_server = HTTPServer(app)
            url = urlparse(os.environ['JUPYTERHUB_SERVICE_URL'])

            http_server.listen(url.port)

            IOLoop.current().start()

        if __name__ == '__main__':
            main()
    ocm-service.py:
      mountPath: /usr/local/etc/jupyterhub/ocm-service.py
      stringData: |
        """OCM webapp-sharing endpoints for JupyterHub.

        POST /services/ocm/shares
            Back channel from the paired Nextcloud's integration_jupyterhub app.
            Body is an OCM share-creation JSON with sharedSecret replaced by
            clientId inside each protocol entry. RFC 9421 signed via the "ocm"
            label using the sender's JWKS. Stored keyed by (sender-domain,
            clientId).

        POST /services/ocm/open
            OCM webapp-sharing entrypoint. The receiver posts the spec-mandated
            form fields (access_token, access_token_ttl). The JWT is verified
            against the issuer's JWKS; the stored share is looked up by
            (iss-domain, JWT.client_id) and owner/shareWith identities are
            cross-checked.
        """
        import base64
        import hashlib
        import html as html_mod
        import json
        import os
        import threading
        import time
        import urllib.parse
        from dataclasses import dataclass
        from urllib.parse import quote, urlparse

        import http_sfv
        import jwt
        import requests
        from cryptography.exceptions import InvalidSignature
        from cryptography.hazmat.primitives import hashes
        from cryptography.hazmat.primitives.asymmetric import ec, ed25519, padding, rsa, utils as asym_utils
        from jwt import PyJWKClient
        from tornado.httpserver import HTTPServer
        from tornado.ioloop import IOLoop
        from tornado.web import Application, HTTPError, RequestHandler

        DEBUG = os.environ.get('NEXTCLOUD_DEBUG_OAUTH', 'false').lower() in ('true', '1', 'yes')
        JWKS_CLIENT_TTL = int(os.environ.get('OCM_JWKS_TTL', '300'))
        SHARE_RECORD_TTL = int(os.environ.get('OCM_SHARE_TTL', '86400'))
        SIG_FRESHNESS_WINDOW = int(os.environ.get('OCM_SIG_FRESHNESS', '300'))
        REQUIRED_COVERED = ('@method', '@target-uri', 'content-digest', 'content-length', 'date')
        TRUSTED_BACK_CHANNEL_DOMAINS = frozenset(
            d.strip() for d in os.environ.get('OCM_TRUSTED_BACK_CHANNEL_DOMAINS', '').split(',') if d.strip()
        )
        HUB_API_URL = os.environ.get('JUPYTERHUB_API_URL', '').rstrip('/')
        HUB_API_TOKEN = os.environ.get('JUPYTERHUB_API_TOKEN', '')
        OCM_LOGIN_URL = os.environ.get('OCM_LOGIN_URL', '/hub/ocm-login')

        def _hub_request(method, path, **kwargs):
            headers = kwargs.pop('headers', {})
            headers['Authorization'] = f'token {HUB_API_TOKEN}'
            return requests.request(method, f'{HUB_API_URL}{path}', headers=headers, timeout=30, **kwargs)

        def hub_ensure_user(name):
            r = _hub_request('POST', f'/users/{quote(name, safe="")}')
            if r.status_code not in (201, 409):
                raise HTTPError(502, f'failed to create hub user: {r.status_code} {r.text[:200]}')

        def hub_start_named_server(user, server, user_options):
            r = _hub_request('POST', f'/users/{quote(user, safe="")}/servers/{quote(server, safe="")}', json={'name': server, 'user_options': user_options})
            if r.status_code not in (201, 202, 400):
                raise HTTPError(502, f'failed to start named-server: {r.status_code} {r.text[:200]}')

        def log(msg):
            if DEBUG:
                with open('/proc/1/fd/1', 'a') as stdout:
                    print(f'[ocm] {msg}', file=stdout)

        _jwks_clients = {}

        def get_jwks_client(domain):
            now = time.time()
            cached = _jwks_clients.get(domain)
            if cached and now - cached[1] < JWKS_CLIENT_TTL:
                return cached[0]
            client = PyJWKClient(f'https://{domain}/.well-known/jwks.json', cache_keys=True)
            _jwks_clients[domain] = (client, now)
            return client

        def get_jwk_by_kid(domain, kid):
            for k in get_jwks_client(domain).get_jwk_set().keys:
                if k.key_id == kid:
                    return k
            return None

        @dataclass
        class ShareRecord:
            sender_domain: str
            client_id: str
            sender: str
            owner: str
            share_with: str
            name: str
            provider_id: str
            share_type: str
            resource_type: str
            protocol: dict
            created_at: float

        class ShareStore:
            def __init__(self):
                self._lock = threading.Lock()
                self._records = {}
            def put(self, rec):
                with self._lock:
                    self._records[(rec.sender_domain, rec.client_id)] = rec
            def get(self, sender_domain, client_id):
                now = time.time()
                with self._lock:
                    rec = self._records.get((sender_domain, client_id))
                    if rec is None:
                        return None
                    if now - rec.created_at > SHARE_RECORD_TTL:
                        del self._records[(sender_domain, client_id)]
                        return None
                    return rec

        store = ShareStore()

        def _derived(name, method, target_uri):
            parts = urlparse(target_uri)
            if name == '@method': return method.upper()
            if name == '@target-uri': return target_uri
            if name == '@authority':
                host = (parts.hostname or '').lower()
                port = parts.port
                scheme = (parts.scheme or '').lower()
                if port is not None and not ((scheme == 'https' and port == 443) or (scheme == 'http' and port == 80)):
                    return f'{host}:{port}'
                return host
            if name == '@scheme': return (parts.scheme or '').lower()
            if name == '@path': return parts.path or '/'
            if name == '@query': return f'?{parts.query}' if parts.query else ''
            if name == '@request-target':
                return (parts.path or '/') + (f'?{parts.query}' if parts.query else '')
            raise HTTPError(400, f'unsupported derived component: {name}')

        def _normalize_field(value):
            return ' '.join(value.strip().split())

        def build_signature_base(method, target_uri, headers, components, sig_params_line):
            lower_headers = {k.lower(): v for k, v in headers.items()}
            lines = []
            for comp in components:
                if comp.startswith('@'):
                    value = _derived(comp, method, target_uri)
                else:
                    if comp.lower() not in lower_headers:
                        raise HTTPError(400, f'missing field for signature: {comp}')
                    value = _normalize_field(lower_headers[comp.lower()])
                lines.append(f'"{comp}": {value}')
            lines.append(f'"@signature-params": {sig_params_line}')
            return '\n'.join(lines).encode('ascii')

        def verify_content_digest(header_value, body):
            matched = False
            for entry in header_value.split(','):
                entry = entry.strip()
                if not entry or '=' not in entry:
                    continue
                alg, value = entry.split('=', 1)
                alg = alg.strip().lower()
                value = value.strip()
                if not (value.startswith(':') and value.endswith(':')):
                    continue
                try:
                    digest_bytes = base64.b64decode(value[1:-1], validate=True)
                except Exception:
                    continue
                if alg == 'sha-256':
                    expected = hashlib.sha256(body).digest()
                elif alg == 'sha-512':
                    expected = hashlib.sha512(body).digest()
                else:
                    continue
                if not hashlib.compare_digest(expected, digest_bytes):
                    return False
                matched = True
            return matched

        _JOSE_TO_NATIVE = {
            'EdDSA': 'ed25519', 'ES256': 'ecdsa-p256-sha256', 'ES384': 'ecdsa-p384-sha384',
            'RS256': 'rsa-v1_5-sha256', 'RS384': 'rsa-v1_5-sha384', 'RS512': 'rsa-v1_5-sha512',
        }
        _NATIVE_ALGS = set(_JOSE_TO_NATIVE.values())

        def normalize_alg(alg):
            lower = alg.lower()
            if lower in _NATIVE_ALGS: return lower
            if alg in _JOSE_TO_NATIVE: return _JOSE_TO_NATIVE[alg]
            raise HTTPError(401, f'unsupported signature algorithm: {alg}')

        def _ecdsa_raw_to_der(raw, coord_size):
            if len(raw) != coord_size * 2:
                raise HTTPError(400, 'ECDSA signature wrong length')
            r = int.from_bytes(raw[:coord_size], 'big')
            s = int.from_bytes(raw[coord_size:], 'big')
            return asym_utils.encode_dss_signature(r, s)

        def verify_signature_primitive(native_alg, public_key, signature, base):
            if native_alg == 'ed25519':
                if not isinstance(public_key, ed25519.Ed25519PublicKey):
                    raise HTTPError(401, 'ed25519 alg requires an OKP/Ed25519 key')
                public_key.verify(signature, base); return
            if native_alg.startswith('rsa-v1_5-'):
                if not isinstance(public_key, rsa.RSAPublicKey):
                    raise HTTPError(401, 'rsa-v1_5 alg requires an RSA key')
                hash_alg = {'rsa-v1_5-sha256': hashes.SHA256(), 'rsa-v1_5-sha384': hashes.SHA384(), 'rsa-v1_5-sha512': hashes.SHA512()}[native_alg]
                public_key.verify(signature, base, padding.PKCS1v15(), hash_alg); return
            if native_alg.startswith('ecdsa-'):
                if not isinstance(public_key, ec.EllipticCurvePublicKey):
                    raise HTTPError(401, 'ecdsa alg requires an EC key')
                coord = 32 if native_alg == 'ecdsa-p256-sha256' else 48
                hash_alg = hashes.SHA256() if native_alg == 'ecdsa-p256-sha256' else hashes.SHA384()
                public_key.verify(_ecdsa_raw_to_der(signature, coord), base, ec.ECDSA(hash_alg)); return
            raise HTTPError(401, f'unsupported signature algorithm: {native_alg}')

        def verify_ocm_signature(handler, body, sender_domain):
            sig_input_hdr = handler.request.headers.get('Signature-Input')
            sig_hdr = handler.request.headers.get('Signature')
            if not sig_input_hdr or not sig_hdr:
                raise HTTPError(401, 'missing Signature-Input or Signature header')
            try:
                sig_input_dict = http_sfv.Dictionary(); sig_input_dict.parse(sig_input_hdr.encode('ascii'))
                sig_dict = http_sfv.Dictionary(); sig_dict.parse(sig_hdr.encode('ascii'))
            except Exception as e:
                raise HTTPError(400, f'malformed signature header: {e}')
            if 'ocm' not in sig_input_dict or 'ocm' not in sig_dict:
                raise HTTPError(401, 'no "ocm"-labeled signature')
            if sig_input_hdr.count('ocm=') > 1 or sig_hdr.count('ocm=') > 1:
                raise HTTPError(401, 'multiple "ocm" signatures present')
            inner_list_item = sig_input_dict['ocm']
            covered = [item.value for item in inner_list_item.value]
            params = dict(inner_list_item.params)
            keyid = params.get('keyid'); alg = params.get('alg'); created = params.get('created')
            if not keyid or not alg or created is None:
                raise HTTPError(401, 'signature params must include keyid, alg, created')
            for required in REQUIRED_COVERED:
                if required not in covered:
                    raise HTTPError(401, f'signature must cover {required}')
            now = int(time.time())
            if abs(now - int(created)) > SIG_FRESHNESS_WINDOW:
                raise HTTPError(401, 'signature created outside freshness window')
            if '#' not in keyid:
                raise HTTPError(401, 'keyid must be "<domain>#<kid>"')
            keyid_domain, kid = keyid.rsplit('#', 1)
            if keyid_domain != sender_domain:
                raise HTTPError(401, f'keyid domain {keyid_domain} != sender domain {sender_domain}')
            digest_hdr = handler.request.headers.get('Content-Digest')
            if not digest_hdr:
                raise HTTPError(400, 'Content-Digest header missing')
            if not verify_content_digest(digest_hdr, body):
                raise HTTPError(400, 'Content-Digest mismatch')
            native_alg = normalize_alg(str(alg))
            jwk = get_jwk_by_kid(sender_domain, str(kid))
            if jwk is None:
                raise HTTPError(401, f'kid {kid} not found in {sender_domain} JWKS')
            if jwk.algorithm_name and normalize_alg(jwk.algorithm_name) != native_alg:
                raise HTTPError(401, 'algorithm sources disagree')
            sig_params_line = str(inner_list_item).strip()
            base = build_signature_base(
                method=handler.request.method,
                target_uri=handler.request.full_url(),
                headers=dict(handler.request.headers.get_all()),
                components=list(covered),
                sig_params_line=sig_params_line,
            )
            sig_value = sig_dict['ocm'].value
            if not isinstance(sig_value, (bytes, bytearray)):
                raise HTTPError(400, 'signature value must be a byte sequence')
            try:
                verify_signature_primitive(native_alg, jwk.key, bytes(sig_value), base)
            except InvalidSignature:
                raise HTTPError(401, 'signature verification failed')

        def verify_access_token(token):
            try:
                unverified = jwt.decode(token, options={'verify_signature': False})
            except jwt.InvalidTokenError as e:
                raise HTTPError(401, f'malformed token: {e}')
            issuer = unverified.get('iss')
            if not issuer:
                raise HTTPError(401, 'token missing iss claim')
            parsed = urlparse(issuer)
            if parsed.scheme != 'https' or not parsed.netloc:
                raise HTTPError(401, 'iss must be an https URL')
            try:
                signing_key = get_jwks_client(parsed.netloc).get_signing_key_from_jwt(token).key
            except Exception as e:
                log(f'JWKS lookup failed for {parsed.netloc}: {e}')
                raise HTTPError(401, 'could not resolve signing key')
            try:
                claims = jwt.decode(
                    token, signing_key,
                    algorithms=['RS256', 'RS384', 'RS512', 'ES256', 'ES384', 'EdDSA'],
                    issuer=issuer,
                    options={'require': ['iss', 'sub', 'aud', 'exp', 'client_id'], 'verify_aud': False},
                )
            except jwt.InvalidTokenError as e:
                raise HTTPError(401, f'token verification failed: {e}')
            return claims

        def _domain_of_ocm_address(addr):
            if '@' not in addr:
                raise HTTPError(400, f'malformed OCM address: {addr}')
            return addr.rsplit('@', 1)[1]

        def _extract_client_id(protocol):
            if protocol.get('name') != 'multi':
                raise HTTPError(400, 'protocol.name must be "multi"')
            webapp = protocol.get('webapp')
            if not isinstance(webapp, dict) or 'clientId' not in webapp:
                raise HTTPError(400, 'protocol.webapp.clientId missing')
            return str(webapp['clientId'])

        class SharesHandler(RequestHandler):
            def check_xsrf_cookie(self): return
            def post(self):
                body = self.request.body or b''
                try:
                    payload = json.loads(body or b'{}')
                except json.JSONDecodeError:
                    raise HTTPError(400, 'invalid JSON body')
                sender = payload.get('sender')
                if not isinstance(sender, str) or '@' not in sender:
                    raise HTTPError(400, 'missing or malformed sender')
                sender_domain = _domain_of_ocm_address(sender)
                if sender_domain not in TRUSTED_BACK_CHANNEL_DOMAINS:
                    raise HTTPError(403, 'sender domain not in trusted back-channel allowlist')
                verify_ocm_signature(self, body, sender_domain)
                try:
                    protocol = payload['protocol']
                    client_id = _extract_client_id(protocol)
                    rec = ShareRecord(
                        sender_domain=sender_domain, client_id=client_id, sender=sender,
                        owner=str(payload['owner']), share_with=str(payload['shareWith']),
                        name=str(payload.get('name', '')), provider_id=str(payload['providerId']),
                        share_type=str(payload['shareType']), resource_type=str(payload['resourceType']),
                        protocol=protocol, created_at=time.time(),
                    )
                except KeyError as e:
                    raise HTTPError(400, f'missing field: {e.args[0]}')
                store.put(rec)
                log(f'stored share ({sender_domain}, {client_id}) for {rec.share_with}')
                self.set_status(201)
                self.set_header('content-type', 'application/json')
                self.write(json.dumps({'status': 'stored'}))

        class OpenHandler(RequestHandler):
            def check_xsrf_cookie(self): return
            def post(self):
                ct = self.request.headers.get('Content-Type', '')
                if not ct.startswith('application/x-www-form-urlencoded'):
                    raise HTTPError(415, f'unsupported Content-Type: {ct}')
                token = self.get_body_argument('access_token', default=None)
                if not token:
                    raise HTTPError(400, 'access_token missing')
                self.get_body_argument('access_token_ttl', default=None)
                claims = verify_access_token(token)
                iss_domain = urlparse(claims['iss']).netloc
                client_id = claims['client_id']
                rec = store.get(iss_domain, client_id)
                if rec is None:
                    raise HTTPError(404, 'no share record for this token')
                if claims['sub'] != rec.owner:
                    raise HTTPError(403, 'JWT sub does not match stored owner')
                if claims['aud'] != rec.share_with:
                    raise HTTPError(403, 'JWT aud does not match stored shareWith')
                log(f'opening share ({iss_domain}, {client_id}) for {rec.share_with}')
                username = f'ocm:{rec.share_with}'
                server_name = f'share-{client_id[:12]}'
                webdav = rec.protocol['webdav']; webapp = rec.protocol['webapp']
                hub_ensure_user(username)
                hub_start_named_server(username, server_name, {
                    'ocm_share': {
                        'webdav_uri': webdav['uri'],
                        'bearer': token,
                        'permissions': webdav.get('permissions', ['read']),
                        'resource_name': rec.name,
                        'sharer': rec.owner,
                        'provider_id': rec.provider_id,
                    },
                })
                next_url = f'/user/{quote(username, safe=":")}/{quote(server_name, safe="")}/'
                target = webapp.get('target', ['iframe'])
                if isinstance(target, list):
                    target = target[0] if target else 'iframe'
                self._render_handoff(token, next_url, target, rec.name)

            def _render_handoff(self, access_token, next_url, target, share_name):
                if target == 'popup':
                    form_target = '_blank'
                elif target == 'iframe':
                    form_target = 'ocm-app-frame'
                else:
                    form_target = '_top'
                title = html_mod.escape(f'Opening {share_name}')
                action = html_mod.escape(OCM_LOGIN_URL, quote=True)
                at_attr = html_mod.escape(access_token, quote=True)
                next_attr = html_mod.escape(next_url, quote=True)
                body_parts = []
                if target == 'iframe':
                    body_parts.append(
                        '<iframe name="ocm-app-frame" id="ocm-app-frame" style="width:100vw;height:100vh;border:0"></iframe>'
                    )
                body_parts.append(
                    f'<form id="f" method="POST" action="{action}" target="{form_target}">'
                    f'<input type="hidden" name="access_token" value="{at_attr}">'
                    f'<input type="hidden" name="next" value="{next_attr}">'
                    '</form><script>document.getElementById("f").submit()</script>'
                )
                body = ''.join(body_parts)
                self.set_header('Content-Type', 'text/html; charset=utf-8')
                self.write(
                    f'<!doctype html><html><head><title>{title}</title>'
                    '<style>html,body{margin:0;padding:0}</style>'
                    f'</head><body>{body}</body></html>'
                )

        class PingHandler(RequestHandler):
            def get(self):
                self.set_header('content-type', 'application/json')
                self.write(json.dumps({'ping': 1}))

        def main():
            prefix = os.environ['JUPYTERHUB_SERVICE_PREFIX']
            app = Application([
                (urllib.parse.urljoin(prefix, 'open'), OpenHandler),
                (urllib.parse.urljoin(prefix, 'shares'), SharesHandler),
                (prefix + '/?', PingHandler),
            ])
            url = urlparse(os.environ['JUPYTERHUB_SERVICE_URL'])
            HTTPServer(app).listen(url.port)
            IOLoop.current().start()

        if __name__ == '__main__':
            main()
  networkPolicy:
    ingress:
      - ports:
          - port: 8082
        from:
          - podSelector:
              matchLabels:
                hub.jupyter.org/network-access-hub: "true"
      - ports:
          - port: 8083
        from:
          - podSelector:
              matchLabels:
                hub.jupyter.org/network-access-hub: "true"
  service:
    extraPorts:
      - port: 8082
        targetPort: 8082
        name: refresh-token
      - port: 8083
        targetPort: 8083
        name: ocm
  extraEnv:
    NEXTCLOUD_DEBUG_OAUTH: "no"
    NEXTCLOUD_HOST: sunet.drive.test.sunet.se
    JUPYTER_HOST: jupyter.drive.test.sunet.se
    JUPYTERHUB_API_KEY:
      valueFrom:
        secretKeyRef:
          name: jupyterhub-secrets
          key: api-key
    JUPYTERHUB_OCM_API_KEY:
      valueFrom:
        secretKeyRef:
          name: jupyterhub-secrets
          key: ocm-api-key
    OCM_TRUSTED_BACK_CHANNEL_DOMAINS: sunet.drive.test.sunet.se
    OCM_TRUSTED_ISSUER_DOMAINS: sunet.drive.test.sunet.se
    JUPYTERHUB_CRYPT_KEY:
      valueFrom:
        secretKeyRef:
          name: jupyterhub-secrets
          key: crypt-key
    NEXTCLOUD_CLIENT_ID:
      valueFrom:
        secretKeyRef:
          name: nextcloud-oauth-secrets
          key: client-id
    NEXTCLOUD_CLIENT_SECRET:
      valueFrom:
        secretKeyRef:
          name: nextcloud-oauth-secrets
          key: client-secret
    networkPolicy:
      enabled: false
proxy:
  chp:
    networkPolicy:
      egress:
        - to:
            - podSelector:
                matchLabels:
                  app: jupyterhub
                  component: hub
          ports:
            - port: 8082
singleuser:
  image:
    name: docker.sunet.se/drive/jupyter-custom
    tag: lab-4.0.10-sunet2
  storage:
    dynamic:
      storageClass: csi-sc-cinderplugin
  extraEnv:
    JUPYTER_ENABLE_LAB: "yes"
  extraFiles:
    jupyter_notebook_config:
      mountPath: /home/jovyan/.jupyter/jupyter_server_config.py
      stringData: |
        import os
        c = get_config()
        c.NotebookApp.allow_origin = '*'
        c.NotebookApp.tornado_settings = {
            'headers': { 'Content-Security-Policy': "frame-ancestors *;" }
        }
        os.system('/usr/local/bin/nc-sync')
      mode: 0644
